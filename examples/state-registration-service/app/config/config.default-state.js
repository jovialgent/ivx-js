const defaultState = [{
    route: 'sample-html-state.test-embedded',
    rule : {
        conditionOperator: "and",
        conditions: [{
            key : "testCondition",
            is: "equals",
            value: true
        }]
    }
}];

export default defaultState;