const settings = [
    {
        eventName: "animateElement",
        args: {
            element: ".input-state-container",
            animationClasses: "show"
        }
    }
];

export default settings;