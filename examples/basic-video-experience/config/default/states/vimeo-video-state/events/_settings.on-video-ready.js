const settings = [
    {
        eventName: "animateElement",
        args: {
            element: ".video-state-container",
            animationClasses: "show"
        }
    }
];

export default settings;