import states from "./states";
import metadata from "./metadata";

const defaultState = [
    {
        stateId: "ivxjs-analytics-test-video"
    }
]
export default {
    states: states(),
    metadata: metadata(),
    defaultState
}