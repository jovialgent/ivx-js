const settings = [
    {
        eventName: "animateElement",
        args: {
            element: "body",
            animationClasses: "animated fadeOut"
        }
    }
];

export default settings;